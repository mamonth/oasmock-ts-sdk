asyncapi: 3.0.0
info:
  title: Test Async API
  version: 1.0.0
channels:
  updates:
    address: user/updates
    messages:
      update:
        payload:
          type: object
          properties:
            kind:
              type: string
    bindings:
      ws:
        method: GET
operations:
  receiveUpdates:
    action: receive
    channel:
      $ref: '#/channels/updates'